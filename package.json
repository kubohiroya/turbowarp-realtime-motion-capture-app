{
  "name": "multiview-pose",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.11.0",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "pnpm --recursive --if-present run build",
    "check:apps": "pnpm --filter \"./apps/*\" --if-present run check",
    "check:extensions": "node scripts/update-extension-readiness.ts --check && node scripts/check-extension-readiness.ts",
    "format": "node scripts/check-format.ts",
    "lint": "node scripts/check-repository.ts",
    "test": "node scripts/release-snapshot.ts",
    "typecheck": "tsc --noEmit && pnpm --filter \"./packages/*\" --if-present run typecheck",
    "check": "pnpm run lint && pnpm run format && pnpm run typecheck && pnpm run check:packages && pnpm run check:pins && pnpm run check:extensions && pnpm run check:apps && pnpm run check:bundle-order && pnpm run test && pnpm run build",
    "pin:extensions": "node scripts/pin-embedded-extensions.ts --write",
    "check:pins": "node scripts/pin-embedded-extensions.ts",
    "update:readiness": "node scripts/update-extension-readiness.ts",
    "check:bundle-order": "node scripts/check-bundle-order.ts",
    "check:packages": "pnpm --filter \"./packages/*\" --if-present run check",
    "snapshot": "node scripts/release-snapshot.ts --write"
  },
  "devDependencies": {
    "@kubohiroya/sb3-toolchain": "0.12.0",
    "@types/node": "24.13.3",
    "typescript": "5.9.3"
  }
}
