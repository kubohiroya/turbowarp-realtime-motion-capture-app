{
  "name": "@multiview-pose/camera-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node --experimental-strip-types ../../scripts/ensure-source-directories.ts camera-app && sb3-toolchain build source --output dist/camera-app.sb3 --yes",
    "check": "node --experimental-strip-types ../../scripts/ensure-source-directories.ts camera-app && sb3-toolchain check source"
  }
}
