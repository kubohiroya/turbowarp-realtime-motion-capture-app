{
  "name": "@turbowarp-realtime-motion-capture-app/pose-3d-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "node scripts/clean-dist.ts && vite build",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build"
  },
  "devDependencies": {
    "@kubohiroya/vite-plugin-turbowarp-extension": "0.5.0",
    "@types/node": "24.13.3",
    "typescript": "5.9.3",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
