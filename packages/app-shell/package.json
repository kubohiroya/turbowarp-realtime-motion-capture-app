{
  "name": "@turbowarp-realtime-motion-capture-app/app-shell",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "node scripts/clean-dist.ts && vite build --config vite.camera.config.ts && vite build --config vite.fusion.config.ts && vite build --config vite.local.config.ts",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build"
  },
  "dependencies": {
    "@kubohiroya/turbowarp-app-shell": "0.2.0"
  },
  "devDependencies": {
    "@kubohiroya/turbowarp-camera-source": "0.11.0",
    "@kubohiroya/vite-plugin-turbowarp-extension": "0.5.0",
    "@types/node": "24.13.3",
    "typescript": "5.9.3",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
